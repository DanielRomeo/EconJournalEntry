// import { Ref } from "react"
import TopNavbar from "./topNavbar"
import SideNavbar from "./sideNavbar"

const Wrapper = ({ ref }: any) => {
    return (
      <div>
        {/* <SideNavbar ref={ref} />
        <TopNavbar forwardedRef={ref} /> */}
      </div>
    );
  };

export default Wrapper;

  